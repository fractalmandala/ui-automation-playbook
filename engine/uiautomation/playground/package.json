{
	"name": "@fractal/uiautomation-playground",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Renders the pipeline's generated components against real bits-ui, with a live inspector for roles, tokens and gate results.",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview"
	},
	"devDependencies": {
		"@sveltejs/vite-plugin-svelte": "^7.3.0",
		"sass": "^1.103.1",
		"svelte": "^5.56.10",
		"vite": "^7.1.0"
	},
	"dependencies": {
		"bits-ui": "^2.19.2"
	}
}
